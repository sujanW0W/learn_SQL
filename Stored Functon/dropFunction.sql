
-- Drop function using DROP clause.
-- IF EXISTS is optional. It is useful for preventing the error. If the function does not exists, it will rather throw a warning, not an error. 

DROP FUNCTION IF EXISTS LEVELFUNCTION;

SHOW WARNINGS;